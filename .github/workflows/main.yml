name: My First Action             # The name shown in the GitHub Actions tab
on: [push]                        # TRIGGER: Run this every time code is pushed

jobs:
  say-hello:                      # Job ID
    runs-on: ubuntu-latest        # RUNNER: The virtual machine type to use
    steps:
      - name: Checkout Code       # Step 1: Download your code onto the runner
        uses: actions/checkout@v4

      - name: Run a Script        # Step 2: Run a terminal command
        run: echo "Hello, GitHub Actions!"
